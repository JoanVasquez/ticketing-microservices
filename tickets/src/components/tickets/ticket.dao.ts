import { BadRequestError } from "@jvtickets22/common";
import Ticket from "./ticket.model";

export const createTicketDao = async (data: any) => {};
export const listTicketsDao = async () => {};
export const listTicketByIdDao = async (ticketId: number) => {};
export const updateTicketDao = async (data: any) => {};
